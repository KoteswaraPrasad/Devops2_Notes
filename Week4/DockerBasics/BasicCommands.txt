docker --version
docker info
docker history httpd
docker pull httpd
docker image
docker run (options) image (argument)

options :-

-it -> runs the container in interactive mode, allowing you to interact with the container through the command line
-d -> runs the container in the background
-p -> port number mapping


Running a container with a specific name :
    docker run --name my-container httpd echo "hello world"

Environment Variables 
Need of Environment Variables

    Without -e :
        Configuration must be hard-coded in the image
        Need to rebuild image for every change
    
    With -e :


Setting Environment :
    
    docker run -e MY_VAR=value httpd env
    this command sets an Environment Variable "MY_VAR" to the value "value" and runs a new container from the httpd image

Multiple Environment Variables

Passing Environment Variable from Host System

docker run -d -p 8080:80 --name my_web httpd

8080 port for our host machine
80 port inside container


docker run -d -p 3307:3306 --name mysql-test -e MYSQL_ROOT_PASSWORD=root123 mysql

-d -> detached mode
--name -> naming the container 
-e -> setting Environment Variable

docker exec -it mysql-test bash

mysql -h 127.0.0.1 -P 3006 -u root -P

docker ps -> to list all the running docker containers

TASK 1 :
    -> Run Ubuntu container 
    -> Pass -e COLLEGE=CSE 
    -> Show echo $COLLEGE
    ->Stop container then check what happened


docker ps -a -> to list all the docker containers running/exited/stopped with container details

docker exec -it container_id bash/env -> Access the docker container and run commands inside the container in bash

docker rm container_id -> to remove stopped container
docker rm -f container_id -> to remove running containers
docker rmi image_id -> to remove image with no containers
docker rmi -f image_id -> removing image with running containers forcefully


Task :

Inside this same running cotainer you are required to :

-create a new directory named /project 
(docker exec -it <container_name_or_id> /bin/bash)
(mkdir /project)

-Inside /project , create a file called report.txt
-add the following text inside the file:
This is our container interaction with our host machine
(echo "This is our container interaction with our host machine" > /project/report.txt)

-Verify the file content
(cat /project/report.txt)

-Copy report.txt from the container to your desktop
(docker cp <container_name>:/project/report.txt C:\Users\Koteswara_prasad\OneDrive\Desktop)

-Create another file named notes.txt in the same /project directory
-Append the text :
(echo "This is notes.txt" >> /project/notes.txt)

-Verify that both files exist inside /project
(ls /project)



Practice Questions:

1) You need to start a new container using the nginx image while setting an environment Variable
ENV_MODE=production 
Write the docker run command to achieve this.

(docker -d -p 8080:81 --name my_nginx -e ENV_MODE=production nginx)

2) You have a running container named my_app, but its not behaving as expected.You want to check its logs to debug any errors.
Write the command to view its logs and follow new log entries in real time

(docker logs -f container_name_or_id)



docker network ls

docker network inspect bridge

docker network create my_bridge_net

docker run -d --name container1 --network my_bridge_net nginx
docker run -d --name container2 --network my_bridge_net alpine sleep 300

docker exec -it container2 ping container1 


Create Volume - docker volume create my_volume


GHCR (GitHub Container Registry) is Docker container registry service provided by GitHub. 
It allows you to store, manage, and share Docker container images directly within your GitHub account.